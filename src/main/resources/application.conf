# Configuration file for the service.
database {
  driver = "org.postgresql.Driver"
  driver = ${?JDBC_DRIVER}
  url    = "jdbc:postgresql://localhost:5432/db"
  url    = ${?JDBC_URL}
  user   = "app"
  user   = ${?JDBC_USER}
  pass   = "1"
  pass   = ${?JDBC_PASS}
}
service {
  host = "127.0.0.1"
  port = 8080
}
